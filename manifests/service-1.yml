apiVersion: v1
kind: Service
metadata:
    name: "aksdemodefender-403e"
    labels:
        app: "aksdemodefender-403e"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aksdemodefender-403e"